spring:
  application:
    name: courier-management
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  datasource:
    url: jdbc:postgresql://localhost:5433/courierdb
    username: postgres
    password: postgres